export interface UserI {
  id: number;
  name: string;
  email: string;
  password: string;
  role?: string; // optional
}
export interface UserI {
    id: number;
    name: string;
    email: string;
    password: string;
    role?: string; // optional role field
}

