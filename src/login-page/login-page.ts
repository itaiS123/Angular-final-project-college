import { LoginPageComponent } from '../app/login-page/login-page.component';

// Backwards-compat alias so older imports still work. Prefer LoginPageComponent
export { LoginPageComponent as LoginPage };
